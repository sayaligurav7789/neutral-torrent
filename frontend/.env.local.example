# ── Backend API URL ────────────────────────────────────────────────────────────
# URL of the Neural Torrent backend server. Must be prefixed with VITE_ so that
# Vite bundles it into the browser build.
VITE_BACKEND_URL=http://localhost:3001

# ── Chain configuration ────────────────────────────────────────────────────────
# Chain ID for the network your ReputationTracker is deployed on.
#   Ethereum Sepolia : 11155111
#   Avalanche Fuji   : 43113
VITE_CHAIN_ID=11155111

# ── Contract addresses (filled after deployment) ───────────────────────────────
# Address of the deployed ReputationTracker contract.
VITE_REPUTATION_TRACKER_ADDRESS=

# ── Admin secret (optional — only needed to use the Migrate button) ────────────
# Must match ADMIN_SECRET in backend/.env.
VITE_ADMIN_SECRET=
