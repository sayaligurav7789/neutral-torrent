# ── Network selection ─────────────────────────────────────────────────────────
# Set ONE of the RPC URL variables below and set CHAIN_ID to match.
# The backend resolves RPC_URL first, then ETH_SEPOLIA_RPC_URL, then
# AVALANCHE_FUJI_RPC_URL, so you can use any of the three variable names.

# Generic (takes precedence over the network-specific vars below)
RPC_URL=https://rpc.sepolia.org

# Ethereum Sepolia (chain 11155111)
# ETH_SEPOLIA_RPC_URL=https://rpc.sepolia.org

# # Avalanche Fuji (chain 43113)
# AVALANCHE_FUJI_RPC_URL=https://api.avax-test.network/ext/bc/C/rpc

# Explicitly set the chain ID to match the RPC URL above:
#   Ethereum Sepolia : 11155111
#   Avalanche Fuji   : 43113
CHAIN_ID=11155111

# ── Deployer wallet ────────────────────────────────────────────────────────────
# NEVER commit a real private key.  Use a dedicated test-only wallet.
DEPLOYER_PRIVATE_KEY=your_private_key_here

# ── Contract addresses (filled after deployment) ───────────────────────────────
REPUTATION_TRACKER_ADDRESS=
FACTORY_ADDRESS=

# ── Backend configuration ──────────────────────────────────────────────────────
PORT=3001
NODE_ENV=development

# Admin secret for the /migrate endpoint — keep this secret and out of VCS.
ADMIN_SECRET=your_admin_secret_here

# Minimum upload/download ratio to grant peer list access (default: 0.5)
# MIN_RATIO=0.5

# Receipt freshness window in seconds (default: 300 = 5 min)
# TIMESTAMP_WINDOW_SECONDS=300

# ── Frontend (Vite) ────────────────────────────────────────────────────────────
VITE_BACKEND_API_URL=http://localhost:3001
VITE_CHAIN_ID=11155111
VITE_REPUTATION_TRACKER_ADDRESS=

# ── Contract verification API keys ────────────────────────────────────────────
# Etherscan (Sepolia)
ETHERSCAN_API_KEY=
# Snowtrace (Fuji)
SNOWTRACE_API_KEY=