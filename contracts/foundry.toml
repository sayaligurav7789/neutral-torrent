[profile.default]
src = "src"
out = "out"
libs = ["lib"]
optimizer = true
optimizer_runs = 200

# RPC endpoints — values are read from the environment at runtime.
# Set FUJI_RPC and SEPOLIA_RPC in contracts/.env before running deploy targets.
[rpc_endpoints]
fuji     = "${FUJI_RPC}"
sepolia  = "${SEPOLIA_RPC}"

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options
